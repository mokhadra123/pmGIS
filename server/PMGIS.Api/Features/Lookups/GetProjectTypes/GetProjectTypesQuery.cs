namespace PMGIS.Api.Features.Lookups.GetProjectTypes;


// Takes no input. It exists so the slice has the same shape as every other one and so a
// filter can be added later without changing the handler signature.
public sealed record GetProjectTypesQuery;
